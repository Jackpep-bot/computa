"""computa — a small, safe toolkit to scan your computer and help it run faster.

Cross-platform (Windows / macOS / Linux). Works with only the Python standard
library; if the optional ``psutil`` package is installed, scans become richer
(live CPU/memory, per-process detail, swap, boot time).
"""

__version__ = "0.1.0"
